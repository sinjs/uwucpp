# Hello world example for UwUCpp

## Compilation:

```sh
$ git clone https://github.com/sinmineryt/uwucpp.git   # Clone the UwUCpp Repository
$ gcc -Iuwucpp -Wall main.c -o main                    # Compile using GCC and output main file
```
